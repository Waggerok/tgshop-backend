// const Router = require('express');
// const router = new Router();

// router.get('/:telegram_id', basketController.getBasketByUser);
// router.post('/:telegram_id/devices', basketController.addDeviceToBasket);
// router.delete('/:telegram_id/devices/:device_id', basketController.removeDeviceFromBasket);
// router.patch('/:telegram_id/devices/:device_id', basketController.updateDeviceQuantity);

// module.exports = router;